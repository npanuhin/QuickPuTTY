{
	// This command is used, to run PuTTY (You can specify path to the executable)
	"PuTTY_exec": "putty",

	// Enable the "Open PuTTY" option (just open the app without any session) in the nav-bar
	"PuTTY_launch_button": true
}
